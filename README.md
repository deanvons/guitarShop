 Build App shell (layout, theme, error boundary, global loaders).

 Build Nav (brand, links to List/Login, active link highlight, responsive menu).

 Build GuitarListPage

Build GuitarListComponent

 Build GuitarListItem Component

 Fetch guitars (mock API first).

 List states: loading, empty, error, success.

 Search by name/brand.

 Filters (type, price range) + reset.

 Sort (price, popularity, A–Z).

 Pagination / infinite scroll.

 Grid/list toggle.

 Build GuitarCard (image, name, price, tags, “view details”).

 Build GuitarDetailsPage

 Load by :id route param.

 Gallery, specs, description.

 Availability badge.

 Related guitars carousel.

 Actions: add to favorites / compare.

 Build GuitarDetails component (reusable details block used by page).

 Build LoginPage

 Email/password form with validation.

 “Remember me” + show/hide password.

 Auth error messages.

 Redirect back to last page on success.

 Build Login widget (mini login/logout button + avatar in Nav).

 Add Protected Routes (details page actions require auth).

 Add Favorites feature (save, list in Nav, persist to storage).

 Add Compare feature (select up to N guitars, compare view).

 Add Toast/Alerts (success/error/info).

 Add NotFound (404) and Unauthorized (401) pages.

 Add Accessibility passes (labels, keyboard nav, focus traps, alt text).

 Add Responsive layouts (mobile → desktop).

 Add Skeletons/Shimmers for list and details loading.

 Add Error-retry controls on failed fetches.

 Add Analytics hooks (page views, key actions).

 Seed Mock data and swap to real API when ready.